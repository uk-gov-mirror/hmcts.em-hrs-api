spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/em-hrs-api
      aliases:
#        microservicekey-em-hrs-api: S2S_KEY
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        hrs-api-POSTGRES-PASS: SPRING_DATASOURCE_PASSWORD
        storage-account-primary-connection-string: azure.storage.connection-string
#        notification-api-key: NOTIFICATION_API_KEY
